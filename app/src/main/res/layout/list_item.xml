<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="meal"
            type="com.develop.nowasteinmyfridge.model.Meal" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">


        <ImageView
            android:id="@+id/ivBgItem"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:radius="30dp"
            android:scaleType="centerCrop"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/bgitem" />

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/civFoodItem"
            android:layout_width="90dp"
            android:layout_height="100dp"
            android:src="@drawable/apple"
            app:civ_border_color="#72A05C"
            app:civ_border_width="9dp"
            app:iconUrl="@{meal.strMealThumb}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:ignore="MissingConstraints" />

        <TextView
            android:id="@+id/tvItemName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="30dp"
            android:layout_marginTop="35dp"
            android:text="@{meal.strMeal}"
            android:textColor="#6A3B10"
            android:textSize="20sp"
            android:textStyle="bold"
            tools:text="Name"
            app:layout_constraintStart_toEndOf="@+id/civFoodItem"
            app:layout_constraintTop_toTopOf="@+id/ivBgItem" />

        <TextView
            android:id="@+id/tvDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="30dp"
            android:layout_marginTop="15dp"
            android:text="1 day ago"
            android:textColor="#6A3B10"
            android:textSize="10sp"
            tools:text="Date Add"
            app:layout_constraintStart_toEndOf="@+id/civFoodItem"
            app:layout_constraintTop_toBottomOf="@+id/tvItemName" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>